// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model users {
  id         Int           @id @default(autoincrement())
  userEmail      String        @unique
  userName   String?
  userPassword   String?
}

model organizers {
  id         Int           @id @default(autoincrement())
  organizerEmail      String        @unique
  organizerName   String?
  organizerPassword   String?
  tournaments tournaments[]
}

model tournaments {
  tournamentId   Int       @id @default(autoincrement())
  tournamentName String
  teamsName      String[]
  organizer      organizers     @relation(fields: [organizerId], references: [id])
  organizerId    Int
  teams          teams[]
  matches        matches[]
}

model teams {
  id             Int         @id @default(autoincrement())
  teamName       String
  tournamentName String?
  tournament     tournaments @relation(fields: [tournamentId], references: [tournamentId])
  tournamentId   Int
  playersName    String[]
  matches        matches[]

  @@unique([id, teamName])
}

model player {
  id         Int       @id @default(autoincrement())
  playerName String
  age        Int
  matches    matches[]

  @@unique([id, playerName])
}

model matches {
  id             Int         @id @default(autoincrement())
  tournamentName String?
  tournament     tournaments @relation(fields: [tournamentId], references: [tournamentId])
  tournamentId   Int
  players        player     @relation(fields: [playerId, playerName], references: [id, playerName])
  playerId       Int
  playerName     String
  team           teams       @relation(fields: [teamId, teamName], references: [id, teamName])
  teamId         Int
  teamName       String
  Runs           Int
  Wickets        Int
  Balls          Int
  Overs          String
}
